import { MedicineWithoutPhoto } from './medicine-without-photo';

describe('MedicineWithoutPhoto', () => {
  it('should create an instance', () => {
    expect(new MedicineWithoutPhoto()).toBeTruthy();
  });
});
